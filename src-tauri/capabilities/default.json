{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidades padrão do MaxMusicBox (kiosk)",
  "windows": ["main", "monitor"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:event:default",
    "dialog:default",
    "opener:default"
  ]
}
